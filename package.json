{
  "name": "exams-portal-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Portal de Agendamento de Exames — frontend Next.js + backend NestJS",
  "scripts": {
    "prepare": "husky",
    "lint": "npm --prefix frontend run lint && npm --prefix backend run lint",
    "test": "npm --prefix frontend run test:run && npm --prefix backend test",
    "format:check": "npm --prefix backend run format -- --check"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.6.0",
    "@commitlint/config-conventional": "^19.6.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.2.10"
  },
  "lint-staged": {
    "frontend/**/*.{ts,tsx}": [
      "bash -c 'cd frontend && npm run lint -- --fix'"
    ],
    "backend/**/*.ts": [
      "bash -c 'cd backend && npm run lint'"
    ]
  },
  "commitlint": {
    "extends": ["@commitlint/config-conventional"]
  }
}
